import type { NextAuthConfig } from "next-auth";
export const authConfig = { pages: { signIn: "/login" }, callbacks: { authorized({ auth, request: { nextUrl } }) { const loggedIn = Boolean(auth?.user); const protectedPath = ["/bookings","/dashboard","/profile","/alumni/dashboard"].some((p) => nextUrl.pathname.startsWith(p)); if (nextUrl.pathname.startsWith("/admin")) return auth?.user?.role === "admin"; return !protectedPath || loggedIn; } }, providers: [] } satisfies NextAuthConfig;
