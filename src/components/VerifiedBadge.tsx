import { BadgeCheck } from "lucide-react";
export function VerifiedBadge() { return <span className="inline-flex items-center gap-1 text-xs font-semibold text-primary"><BadgeCheck size={16} className="fill-primary text-white" />Verified JBCN alumnus</span>; }
